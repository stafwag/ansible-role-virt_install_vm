---
- name: Install tstdebian13
  hosts: localhost
  become: true
  vars:
    vm:
      hostname:
        tstfreebsd14
      ip_address:
        192.168.122.5/24
  pre_tasks:
    - name: Load the vm template
      ansible.builtin.include_vars: freebsd_14_vm_template.yml
    - name: Display qemu_img
      ansible.builtin.debug:
        msg:
          - 'qemu_img: {{ qemu_img }}'
  roles:
    - stafwag.virt_install_vm
